name=TradesRawDataCassandraConnector
connector.class=com.datamountaineer.streamreactor.connect.cassandra.sink.CassandraSinkConnector
connect.cassandra.key.space=markets
topics=TRADES-BITFINEX,TRADES-BITSTAMP,TRADES-GDAX,TRADES-HITBTC,TRADES-POLONIEX,TRADES-ETHERDELTA
tasks.max=6
connect.cassandra.kcql=INSERT INTO trade SELECT * FROM TRADES-BITFINEX;INSERT INTO trade SELECT * FROM TRADES-BITSTAMP;INSERT INTO trade SELECT * FROM TRADES-GDAX;INSERT INTO trade SELECT * FROM TRADES-HITBTC;INSERT INTO trade SELECT * FROM TRADES-POLONIEX;INSERT INTO trade SELECT * FROM TRADES-ETHERDELTA;
connect.cassandra.fetch.size=5000
connect.cassandra.max.retries=20
connect.cassandra.error.policy=throw
connect.cassandra.contact.points=docker.for.mac.localhost
connect.cassandra.port=9042
connect.cassandra.threadpool.size=10
name=CassandraSinkConnector
connect.cassandra.retry.interval=30000
connect.progress.enabled=true
connect.cassandra.consistency.level=LOCAL_ONE
value.converter=org.apache.kafka.connect.json.JsonConverter
key.converter=org.apache.kafka.connect.json.JsonConverter
value.converter.schemas.enable=false
key.converter.schemas.enable=false